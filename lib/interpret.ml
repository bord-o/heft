(*

The runtime for the object language. Rather than the interactive LCF approach, this interpreter will handle arbitrary
computation within the constraints of the language, resolve any reflective code, and ultimately call the kernel to check
theorems.

 *)
